<div class="container">
    <div class="row">
      <% @books.each do |book| %>
      <div class="col">
        <h3><%= link_to book.book_name, book %></h3>
      </div>
      <% end %>
    </div>
    <%= paginate @books %>
  </div>