<div class="container">
    <div class="row">
      <% @books.each do |book| %>
      <%= paginate @books %>
      <div class="col">
        <p><%= link_to book.name, book%><p>
      </div>
        <% end %>
    </div>
  </div>